version: 1
frontend:
  phases:
    # ... other phases (provision) ...
    preBuild:
      commands:
        # CHANGE THIS LINE from - npm ci to - npm install
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
